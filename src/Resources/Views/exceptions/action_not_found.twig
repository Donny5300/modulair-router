<h1>Action [{{ message.getMessage() ? message.getMessage() : 'index' }}]not found</h1>

{% if fileExists %}
	<p>The file exists, please validate the namespace, controller class name and make sure the path is correct</p>
{% else %}
	<p>File does'nt exists. Would you like to create the file?</p>

	<ul>
		<li>Controller name: {{ controller }}</li>
		<li>Namespace: {{ namespace }}</li>
	</ul>

	<a href="/system/exceptions/action/create?message={{ message.getMessage() }}">Add missing action</a>
{% endif %}